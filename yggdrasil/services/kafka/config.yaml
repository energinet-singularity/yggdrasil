name: kafka
namespace: kafka
namespaceLabels:
  space: service
space: service
description: Kafka.

apps:
  - name: kafka
    replace: true
    source:
      repoURL: "https://github.com/distributed-technologies/helm-charts.git"
      targetRevision: feature/kafka-create-connector
      path: charts/kafka/chart
      valuesFile: "kafka.yaml"
