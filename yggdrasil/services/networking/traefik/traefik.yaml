dashboardRoute:
  host: ingress.{{ .Values.ingress.ingressDomain }}

traefik:
  {{- if .Values.registry }}
  image: 
    name: {{ .Values.registry }}/traefik
  {{- end }}
  ingressClass:
    fallbackApiVersion: "v1"

  service:
    enabled: true
    type: LoadBalancer
    annotations:
      service.beta.kubernetes.io/azure-load-balancer-resource-group: {{ .Values.ingress.loadbalancerResourceGroup | quote }}
    spec: 
      loadBalancerIP: {{ .Values.ingress.loadBalancerIP }}

  ports:
    websecure:
      tls: 
        enabled: true  

  logs:
    access:
      enabled: true
