jaeger-operator:
  provisionDataStore:
    cassandra: false
    elasticsearch: true
    kafka: false
  storage:
    # allowed values (cassandra, elasticsearch)
    type: elasticsearch
  elasticsearch:
    user: admin
    usePassword: true
    password: password

elasticsearch: 
  volumeClaimTemplate: 
    storageClass: "ceph-filesystem"
